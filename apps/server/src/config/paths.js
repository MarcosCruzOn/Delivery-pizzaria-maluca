const path = require('path')

// aponta para a pasta client na raiz do projeto (ajuste se seu client estiver em outro lugar)
const CLIENT_DIR = path.resolve(__dirname, '../../../client')

module.exports = { CLIENT_DIR }
